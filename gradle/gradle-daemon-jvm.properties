# Gradle daemon JVM criteria - Stonecutter 0.9.6 requires the daemon to run on Java 21+.
# Gradle auto-discovers an installed JDK 21 (no hardcoded path, portable across machines), so
# ./gradlew and scripts/*.ps1 work even when JAVA_HOME points at an older JDK (e.g. Java 17).
toolchainVersion=21
